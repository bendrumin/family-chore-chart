<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

            <!-- App Links: chorestar.app links for the app's OWN routes open
                 here instead of the browser (sign-in callback, password
                 reset, family invites, kid login, the dashboard). Marketing
                 pages are deliberately left out: they carry pricing copy the
                 consumption-only shell must never show. Verified against
                 https://chorestar.app/.well-known/assetlinks.json, which must
                 list the certificate that signs the installed build (the
                 upload key for sideloads, Play's app-signing key for Play
                 installs). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="chorestar.app" />
                <data android:pathPrefix="/auth/callback" />
                <data android:pathPrefix="/reset-password" />
                <data android:pathPrefix="/family/accept/" />
                <data android:pathPrefix="/kid-login" />
                <data android:pathPrefix="/kid/" />
                <data android:pathPrefix="/dashboard" />
                <data android:path="/login" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- navigator.vibrate() inside a WebView needs the host app's permission;
         the web app gives chore and routine completions a short haptic tick. -->
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
